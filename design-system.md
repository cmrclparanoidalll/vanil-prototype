# design-system.md — Ваніль

Знято з `assets/style.css` (`:root`) — це опис того, що вже є в коді,
а не побажання. Значення поза цими списками вважаються порушенням.

## Колір

| токен | значення | роль |
|---|---|---|
| `--w` | `#F2EDE3` | основа (ванільна, не біла) |
| `--k` | `#14120E` | теплий чорний: текст, темні поля |
| `--grey` | `#6F675C` | другорядний текст |
| `--hair` | `#DED5C6` | лінії й рамки |
| `--tint` | `#E7DFD0` | ховер і заливка чипів по світлому |
| `--grey-dk` | `#A79C8C` | другорядний текст на темному полі (6,93:1) |
| `--ph` | `#DCD3C4` | тло під фото до завантаження |

Колір також дає фотографія. Акцентного кольору немає навмисно.

## Шкала відступів

```
--s-xs  15   --s-sm  30   --s-md  35
--s-lg  50   --s-xl  115  --s-2xl 154
--g     clamp(20px, 3.5vw, 50px)   поле від краю, одне на весь сайт
--nav   74
```

Вертикальні відступи секцій беруться **тільки** з цієї шкали.
Горизонтальні поля — тільки `--g`.

## Мікрокрок (відступи всередині компонента)

Шкала вище керує **відступами секцій**. Усередині компонента (картка, чип,
поле форми) сайт від початку користується дрібнішим кроком по 5px:
`5 · 10 · 15 · 20 · 25`. Приклади, які вже були в коді до цього документа:
`.figures div{gap:18px}`, `.drawer .acts{gap:12px}`, `.fld{padding:14px 0}`.

**Похідні значення — на системі.** Будь-що, пораховане `calc()` з токена
(`calc(var(--s-sm) * 2)`, `calc(var(--rail-wave) * -1)`) або обчислене в JS
із ширини екрана (центрувальне поле доріжки), порушенням не є: воно
змінюється разом із токеном, а не живе окремо.

## Типографіка

Одна гарнітура (Onest), одна вага (400), `letter-spacing: .15px`.

| роль | кегль |
|---|---|
| тег, надрядок | 12, трекінг .14em, uppercase |
| дрібний підпис | 14 |
| посилання, кнопка | 16 |
| тіло | 18 |
| заголовок секції | 26 |
| цифра | `clamp(68px, 9.5vw, 152px)` |
| цифра дрібна | `clamp(30px, 3.4vw, 46px)` |

Великі заголовки-заяви (`.claim__h`, `.rail__head h2`) — окремий рівень
поверх цього списку; вони мусять мати **один спільний кегль**, а не два різні.

## Форма

`border-radius: 0` на всьому сайті. Рамка — 1px `--k` або `--hair`.
Скруглення допускається лише там, де його явно затверджено (картки рейки, 12px).

## Рух

`prefers-reduced-motion` вимикає все. Поява секцій — 700ms, opacity + 16px зсуву.

## Правило відступу від системи

Значення поза шкалою допускається тільки як явна вимога замовника й
записується в `progress.md` рядком «хто і коли попросив». Інакше — порушення.
