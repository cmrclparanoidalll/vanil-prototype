# progress.md

## Винятки зі шкали, погоджені замовником

Записано за правилом з `design-system.md` («значення поза шкалою допускається
тільки як явна вимога замовника»). Усі — прохання Paul у сесії 2026-08-31:

| значення | де | що просив замовник |
|---|---|---|
| 270px | `.claim` padding-top | «відступ … від картинки 270 пк» |
| 310px | `.claim` середній рядок гріда | «відстань має бути по висоті 310 пк» |
| 376px | `.claim` padding-bottom | дає задані 470px до блоку «Проєкти» |
| 380px | `.rail__head` margin-bottom | «відстань між іншим блоком 380 пк» |
| 150px | `.rail` padding-bottom | «додай 150 пк до низу чорного» |
| −4% | трекінг великих заголовків | «міжбуквенна відстань … −4%» |
| 12px | скруглення карток рейки | «картки скругли кути як на рефі» |

## Раунди

| piece | round | Sys | Brief | Craft | що виправлено |
|---|---|---|---|---|---|
| claim+rail | 1 | FAIL | — | — | кеглі заголовків 84/66 різні, 17px і 15px поза шкалою, скруглення тега |
| claim+rail | 2 | FAIL | — | — | документ не описував мікрокрок компонентів — додано розділ; тег 11→12px |
| claim+rail | 3 | FAIL | — | — | трекінг надрядка .16em → .14em |
| claim+rail | 4 | FAIL | PASS | FAIL | додано токен --grey-dk; Craft: B3 у заявній секції |

## Блокери й наступний крок

- Панель браузера в застосунку періодично згортається і не віддає кадри;
  знімки роблю headless Chrome через CDP (`renders/`), це працює стабільно.
- Стоїть відкритим: Craft B3 — у заявній секції абзац і CTA стоять під
  заголовком, а не поруч. Виправлення суперечить прямій вимозі замовника
  (310px по вертикалі між ними), тому потрібне його рішення.
- Brief просить винести «власне виробництво» і «21 день» у заявну секцію.

## Зміни в design-system.md, спричинені критиками

- **Раунд 2, критик System.** Документ описував лише відступи секцій і не мав
  правила для відступів усередині компонента, хоча код користувався кроком по
  5px від самого початку (`.figures div{gap:18px}`, `.drawer .acts{gap:12px}`).
  Додано розділ «Мікрокрок» і правило про похідні значення (`calc()` з токена
  та обчислене в JS центрувальне поле порушенням не є). До типошкали додано
  рівень «тег, надрядок — 12px / .14em», який уже був у `.claim__eye`.
  Це опис наявної системи, а не легалізація нового числа: тег рейки зведено
  з 11px до тих самих 12px.
- **Гейт, критик System.** У палітрі не було кольору для другорядного тексту на
  темному полі: `--grey` (#6F675C) на `--k` дає 2,3:1 і провалює AA, тому в коді
  жив захардкоджений `#A79C8C`. Додано токен `--grey-dk` (6,93:1) і замінено
  єдине використання. Це закриття діри в палітрі, а не легалізація числа.
